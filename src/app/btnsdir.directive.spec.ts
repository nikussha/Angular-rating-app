import { BtnsdirDirective } from './btnsdir.directive';

describe('BtnsdirDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnsdirDirective();
    expect(directive).toBeTruthy();
  });
});
